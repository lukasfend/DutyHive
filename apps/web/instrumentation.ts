/**
 * Next.js instrumentation — Foundation Phase 5 wires Sentry server/edge init here.
 *
 * Empty for now. The file's existence is required if `experimental.instrumentationHook`
 * is enabled (default in Next 15 — no longer experimental).
 */
export async function register() {
  /* Phase 5: import('@sentry/nextjs').then(...) for nodejs and edge runtimes. */
}
