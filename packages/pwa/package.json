{
  "name": "@dutyhive/pwa",
  "version": "0.0.0",
  "private": true,
  "description": "Serwist-based PWA infrastructure. Per-subdomain manifest factory + opt-in service worker registration.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./register-sw": "./src/register-sw.tsx",
    "./manifest": "./src/manifest.ts"
  },
  "scripts": {
    "lint": "echo 'lint placeholder'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'tests in Phase 5'",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {
    "@dutyhive/config": "workspace:*"
  }
}
