{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // No rootDir/outDir — consumed by source via Next 16's transpilePackages.
    "jsx": "preserve"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
