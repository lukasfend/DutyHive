{
  "name": "@dutyhive/config",
  "version": "0.0.0",
  "private": true,
  "description": "Single source of truth for brand identity, subdomain map, and feature flags. Pure constants, no runtime dependencies.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./brand": "./src/brand.ts",
    "./subdomains": "./src/subdomains.ts",
    "./flags": "./src/flags.ts"
  },
  "scripts": {
    "lint": "echo 'lint placeholder'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests in @dutyhive/config'",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  }
}
