{
  "name": "@dutyhive/jobs",
  "version": "0.0.0",
  "private": true,
  "description": "Trigger.dev v3 task definitions. Reusable from web app and (future) dedicated worker.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo 'lint placeholder'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'tests in Phase 5'",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {
    "@dutyhive/audit": "workspace:*",
    "@dutyhive/db": "workspace:*",
    "@dutyhive/email": "workspace:*",
    "@dutyhive/env": "workspace:*",
    "@dutyhive/logger": "workspace:*",
    "@trigger.dev/sdk": "^4.4.5"
  }
}
