{
  "name": "@dutyhive/auth",
  "version": "0.0.0",
  "private": true,
  "description": "Better Auth server config + organization plugin + RLS-aware session bridge.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./server": "./src/server.ts",
    "./client": "./src/client.ts",
    "./with-tenant": "./src/with-tenant.ts"
  },
  "scripts": {
    "lint": "echo 'lint placeholder'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'tests in Phase 2'",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  }
}
