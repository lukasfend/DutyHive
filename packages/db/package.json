{
  "name": "@dutyhive/db",
  "version": "0.0.0",
  "private": true,
  "description": "Prisma client + RLS-aware transaction wrapper. Multi-tenancy via Postgres Row Level Security.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "db:generate": "echo 'prisma generate (Phase 2)'",
    "db:migrate": "echo 'prisma migrate dev (Phase 2)'",
    "db:seed": "echo 'prisma db seed (Phase 2)'",
    "db:studio": "echo 'prisma studio (Phase 2)'",
    "db:push:test": "echo 'prisma db push test schema (Phase 2)'",
    "lint": "echo 'lint placeholder'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'unit tests (Phase 2)'",
    "test:rls": "echo 'RLS integration tests (Phase 2)'",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  }
}
