{
  "name": "@dutyhive/audit",
  "version": "0.0.0",
  "private": true,
  "description": "Audit-log helper. Explicit calls only — no Prisma middleware magic. PII (IP, UA) hashed with rotating salt.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo 'lint placeholder'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'tests in Phase 2'",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {
    "@dutyhive/db": "workspace:*",
    "@dutyhive/env": "workspace:*"
  }
}
